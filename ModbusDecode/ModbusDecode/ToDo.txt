Work To Do:

- Auto-detect Modicon float ?? (if abs float value is less than 1e-10 ??)
- Auto-detect Master/Slave ??  (if RX 01 10 - write Multiple Register => We are Slave
- Mark bad checksum (B) commands with red color for easier detection
 